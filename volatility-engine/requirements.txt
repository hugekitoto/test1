# Core (required)
pandas>=2.0
numpy>=1.24

# Charts (scripts/run_full.py). Core analysis works without it.
matplotlib>=3.6

# Optional — only needed for live data fetching (ve/fetch.py), run locally.
# yfinance>=0.2
# requests>=2.31
